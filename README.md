# Academic Profile

Репозиторий, объединяющий части одного большого проекта. Состоит из двух частей:
- **Frontend** на Vue.js с Vite (`academic-profile-web`)
- **Backend** на FastAPI (`backend_academic`)

Каждая из частей является собственным Git-репозиторием.

## 🚀 Быстрый старт с Docker Compose

### Предварительные требования

- Docker (версия 20.10 или выше)
- Docker Compose (версия 1.29 или выше)

### Запуск проекта

1. **Клонируйте репозитории** (если еще не сделано):
   ```bash
   git clone <frontend-repo-url> academic-profile-web
   git clone <backend-repo-url> backend_academic
   ```

2. **Убедитесь, что CSV файлы находятся в директории backend**:
   - `backend_academic/authors_expanded_with_ids.csv`
   - `backend_academic/authors_scientific_interests.csv`
https://drive.google.com/drive/folders/1weqZpz9Y24aHnonFIKbn8IJvipeWZN5q?usp=share_link - скачать их можно здесь
3. **Запустите проект через Docker Compose**:
   ```bash
   docker-compose up --build
   ```

   Или в фоновом режиме:
   ```bash
   docker-compose up -d --build
   ```

### Что происходит при первом запуске

При первом запуске Backend автоматически:
1. ✅ Создает базу данных `users.db` (если она не существует)
2. ✅ Импортирует данные из CSV файлов (`import_csv.py`)
3. ✅ Запускает сервер на постоянной основе

### Доступ к приложению

После запуска:
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000
- **API документация**: http://localhost:8000/docs

### Остановка проекта

```bash
docker-compose down
```

Для остановки и удаления volumes (включая базу данных):
```bash
docker-compose down -v
```

## 📁 Структура проекта

```
academic-profile/
├── docker-compose.yml          # Главный файл Docker Compose
├── academic-profile-web/       # Frontend (Vue.js + Vite)
│   ├── Dockerfile
│   ├── .dockerignore
│   └── ...
└── backend_academic/           # Backend (FastAPI)
    ├── Dockerfile
    ├── entrypoint.sh           # Скрипт инициализации
    ├── .dockerignore
    ├── app/
    ├── model/
    └── ...
```

## 🔧 Конфигурация

### Переменные окружения

Backend использует следующие переменные окружения (можно задать в `docker-compose.yml` или через `.env` файл):

- `DATABASE_URL` - URL базы данных (по умолчанию: `sqlite:///./users.db`)
- `SECRET_KEY` - Секретный ключ для JWT токенов
- `ACCESS_TOKEN_EXPIRE_MINUTES` - Время жизни токена (по умолчанию: 60)

Frontend использует:
- `VITE_API_URL` - URL API бэкенда (по умолчанию: `http://localhost:8000`)

### Volumes

- `./backend_academic/users.db` - База данных SQLite
- `./backend_academic/model/` - Модели машинного обучения
- `./academic-profile-web/node_modules` - Зависимости Node.js (исключены из синхронизации)

## 🛠️ Разработка

### Запуск в режиме разработки

Проект настроен для работы в режиме разработки:
- Frontend: Hot Module Replacement (HMR) включен
- Backend: Автоматическая перезагрузка при изменении кода (если используется watch mode)

### Пересборка после изменений

```bash
docker-compose up --build
```

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

## 🔍 Проверка состояния

### Health check

Backend имеет встроенный health check endpoint:
```bash
curl http://localhost:8000/health
```

### Проверка контейнеров

```bash
docker-compose ps
```

## 📝 Примечания

- При первом запуске импорт CSV может занять некоторое время в зависимости от размера файлов
- База данных `users.db` создается автоматически при первом запуске
- Если CSV файлы отсутствуют, импорт будет пропущен, но сервер все равно запустится
- Для продакшена рекомендуется использовать PostgreSQL вместо SQLite

## 🐛 Устранение неполадок

### Проблема: Backend не запускается

1. Проверьте логи: `docker-compose logs backend`
2. Убедитесь, что порт 8000 не занят другим процессом
3. Проверьте наличие всех необходимых файлов в `backend_academic/`

### Проблема: Frontend не подключается к Backend

1. Проверьте, что оба контейнера запущены: `docker-compose ps`
2. Убедитесь, что переменная `VITE_API_URL` правильно настроена
3. Проверьте CORS настройки в backend

### Проблема: Импорт CSV не выполняется

1. Убедитесь, что файлы `authors_expanded_with_ids.csv` и `authors_scientific_interests.csv` находятся в `backend_academic/`
2. Проверьте права доступа к файлам
3. Посмотрите логи: `docker-compose logs backend`

## 📚 Дополнительная документация

- [Backend README](./backend_academic/README.md)
- [Frontend README](./academic-profile-web/README.md)


